
clusterName: "elasticsearch"
nodeGroup: "master"

replicas: 1
minimumMasterNodes: 1

esJavaOpts: "-Xmx512m -Xms512m"

persistence:
  enabled: false

service:
  type: ClusterIP
  ports:
    - port: 9200
      name: http