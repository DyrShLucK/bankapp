# Файл: .\charts\monitoring\values-dev.yaml (корректированный)
prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    ruleSelectorNilUsesHelmValues: false
    resources:
      requests:
        memory: 400Mi
        cpu: 100m
      limits:
        memory: 1Gi
        cpu: 500m
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: standard
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 1Gi
serviceMonitors:
  - name: front-service-bankapp-dev-monitor
    selector:
      matchLabels:
        app.kubernetes.io/name: front-service
        app.kubernetes.io/instance: bankapp-dev
    namespaceSelector:
      matchNames:
        - bankapp-dev
    endpoints:
      - port: http
        path: /actuator/prometheus
        interval: 15s
scrape_configs:
  - job_name: 'front-service'              # Название задачи
    metrics_path: '/actuator/prometheus'    # Путь к эндпоинту с метриками
    scrape_interval: 15s                    # Интервал для получения — каждые 15 секунд
    static_configs:
      - targets: ['bankapp-dev-front-service']         # Адрес (можно указать несколько — например, несколько подов в Kubernetes)
        labels:
          application: 'front-service'
grafana:
  enabled: true
  adminPassword: "admin"
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1Gi
  persistence:
    enabled: false
  ingress:
    enabled: true
    className: "nginx"
    hosts:
      - grafana.dev.bankapp.com
    tls:
      - secretName: grafana-tls
        hosts:
          - grafana.dev.bankapp.com