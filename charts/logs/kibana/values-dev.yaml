# elk/kibana/values.yaml
# Пример минимальной конфигурации
replicas: 1

service:
  type: NodePort # или LoadBalancer, в зависимости от нужд
  nodePort: 30601 # Укажи свободный порт, если type=NodePort

# Указываем адрес Elasticsearch
elasticsearchHosts: "http://elasticsearch-es-http.logging.svc.cluster.local:9200"